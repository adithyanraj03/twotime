target/

# This project never deletes files; quarantines land here and stay out of the tree
Deleted_files/
_future-pushes/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
